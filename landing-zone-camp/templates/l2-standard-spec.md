# L2 标准 Spec 模板

> 用于**单系统内的新模块或结构性变更**（新功能模块、新表/新字段、对接第三方、权限模型修改、功能贯穿 Controller+Service+Repository 等）。
> 分级判定依据见 [handbook/spec-levels.md](../handbook/spec-levels.md)。

---

## 背景
<!-- 为什么做这个？解决什么问题？业务背景一句话。 -->


## 用户故事
<!-- 作为<角色>，我希望<功能>，以便<价值>。 -->


## 验收条件

### 正常流程
<!-- WHEN…THEN… 可断言的事实。至少 2 条。 -->
- WHEN `<条件>` THEN `<行为>`

### 异常流程
<!-- 失败 / 降级 / 权限不足等分支。 -->
- WHEN `<异常条件>` THEN `<降级/错误行为>`

### 边界条件
<!-- 空值 / 上限 / 并发 / 时间边界。 -->
- WHEN `<边界条件>` THEN `<行为>`

## 非功能需求
- **性能**：<!-- 如 P99 < 200ms；批量接口返回 < 1s。具体值，不要"要快"。 -->
- **安全**：<!-- 鉴权方式、输入校验、敏感数据脱敏。 -->
- **幂等**：<!-- 重复调用是否安全、如何保证幂等。 -->

## 依赖与影响范围
- **DB 变更**：<!-- 新表/新字段，或"无"。 -->
- **API 变更**：<!-- 新接口/改接口，或"无"。 -->
- **上下游依赖**：<!-- 依赖哪些服务、影响哪些调用方。 -->


## 回滚方案
<!-- 出问题怎么退：开关、灰度、降级路径。回滚是否丢数据。 -->
