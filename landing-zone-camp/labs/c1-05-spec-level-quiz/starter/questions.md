# 判定以下改动：L0（免 Spec，Commit Message 即记录）还是 L1（快速 Spec）？
1. 修正一个变量名拼写（不影响任何外部行为）
2. 修复列表页在空数据时的 NPE
3. 把某服务日志级别从 INFO 调到 DEBUG
4. 给订单列表新增"按金额排序"选项
5. 某依赖从 2.3.1 升到 2.3.2（补丁版本，无 API 变更）
6. 新增一个"按手机号查用户"的查询接口（无新表）
7. 修复 ESLint 报的格式问题
8. 修改一个 React 组件的点击交互逻辑
9. 提取重复代码为公共方法（有测试保护，行为不变）
10. 把接口超时配置从 3s 调到 5s
