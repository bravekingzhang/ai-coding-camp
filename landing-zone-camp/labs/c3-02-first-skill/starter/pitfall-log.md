# 踩坑记录（原始素材）
上周订单导出接口越用越慢。排查半天：SQL 用 LIMIT 200000, 50 翻页，
OFFSET 越大扫描越多。第一次改成加索引没用（本来就有索引），
后来改成游标翻页 WHERE id > :last_id ORDER BY id LIMIT 50 才解决，P99 从 8s 降到 90ms。
中间还踩了一脚：前端依赖"跳到第 N 页"，改造后要产品确认改为"下一页"交互。
测试要点：翻到最后一页、并发写入时游标不重不漏。
